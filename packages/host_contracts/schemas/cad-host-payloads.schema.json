{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.kythuatvang.local/cad.host/1/payloads.schema.json",
  "title": "cad.host/1 allowlisted read-only payloads",
  "$defs": {
    "handshake": {
      "type": "object",
      "additionalProperties": false,
      "required": ["session_nonce", "agent_version", "protocol_min", "protocol_max"],
      "properties": {
        "session_nonce": { "type": "string", "minLength": 32, "maxLength": 256 },
        "agent_version": { "type": "string", "minLength": 1, "maxLength": 64 },
        "protocol_min": { "const": "cad.host/1" },
        "protocol_max": { "const": "cad.host/1" }
      }
    },
    "handshake_result": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "selected_protocol",
        "host_family",
        "host_version",
        "package_id",
        "package_version",
        "package_hash",
        "session_proof",
        "product",
        "edition",
        "release_year",
        "capabilities"
      ],
      "properties": {
        "selected_protocol": { "const": "cad.host/1" },
        "host_family": { "type": "string", "minLength": 1, "maxLength": 32 },
        "host_version": { "type": "string", "minLength": 1, "maxLength": 64 },
        "package_id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$",
          "maxLength": 128
        },
        "package_version": { "type": "string", "minLength": 1, "maxLength": 64 },
        "package_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
        "session_proof": {
          "type": "string",
          "description": "Lowercase HMAC-SHA256 proof over the negotiated handshake transcript.",
          "pattern": "^[0-9a-f]{64}$"
        },
        "product": { "type": "string", "minLength": 1, "maxLength": 128 },
        "edition": { "enum": ["full", "lt"] },
        "release_year": { "type": "integer", "minimum": 2018, "maximum": 2200 },
        "series": { "type": "string", "minLength": 1, "maxLength": 32 },
        "active_document_id": { "type": ["string", "null"], "maxLength": 128 },
        "capabilities": {
          "type": "array",
          "maxItems": 64,
          "uniqueItems": true,
          "items": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$",
            "maxLength": 64
          }
        }
      }
    },
    "command": {
      "type": "object",
      "additionalProperties": false,
      "required": ["operation_id", "operation_version", "arguments"],
      "properties": {
        "operation_id": {
          "enum": [
            "host.health",
            "drawing.observe.summary",
            "entity.snapshot.page",
            "document.events.summary",
            "cad.program.preview",
            "cad.program.commit",
            "cad.program.validate",
            "cad.recovery.receipt_query",
            "cad.rollback.checkpoint.lookup",
            "cad.rollback.preview",
            "cad.rollback.commit",
            "cad.rollback.validate"
          ]
        },
        "operation_version": { "const": 1 },
        "document_id": { "type": ["string", "null"], "maxLength": 128 },
        "arguments": {
          "type": "object",
          "additionalProperties": false,
          "maxProperties": 8,
          "properties": {
            "include_layers": { "type": "boolean" },
            "max_layers": { "type": "integer", "minimum": 0, "maximum": 256 },
            "cursor": { "type": "integer", "minimum": 0, "maximum": 10000000 },
            "limit": { "type": "integer", "minimum": 1, "maximum": 200 },
            "max_scan": { "type": "integer", "minimum": 1, "maximum": 20000 },
            "expected_revision": { "type": "integer", "minimum": 1 },
            "types": {
              "type": "array",
              "maxItems": 32,
              "uniqueItems": true,
              "items": { "type": "string", "minLength": 1, "maxLength": 64 }
            },
            "layers": {
              "type": "array",
              "maxItems": 32,
              "uniqueItems": true,
              "items": { "type": "string", "minLength": 1, "maxLength": 255 }
            },
            "space": { "enum": ["all", "model", "paper"] },
            "after_sequence": { "type": "integer", "minimum": 0 },
            "max_events": { "type": "integer", "minimum": 1, "maximum": 100 },
            "program": { "$ref": "cad-program-0.2.schema.json" },
            "preview_id": { "type": "string", "minLength": 1, "maxLength": 128 },
            "expires_at": { "type": "string", "format": "date-time", "maxLength": 64 },
            "receipt_id": { "type": "string", "minLength": 1, "maxLength": 128 },
            "checkpoint_id": { "type": "string", "minLength": 1, "maxLength": 128 },
            "checkpoint_digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
            "rollback_plan_id": { "type": "string", "minLength": 1, "maxLength": 128 },
            "rollback_plan_digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
            "rollback_execution_digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
            "rollback_receipt_id": { "type": "string", "minLength": 1, "maxLength": 128 },
            "execution_binding": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "program_digest",
                "execution_digest",
                "document_id",
                "document_revision",
                "runtime_id",
                "runtime_role",
                "host_family",
                "host_version",
                "package_id",
                "package_version",
                "package_hash",
                "capability_manifest_hash",
                "operation_registry_version",
                "operation_registry_hash",
                "policy_version"
              ],
              "properties": {
                "program_digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
                "execution_digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
                "document_id": { "type": "string", "minLength": 1, "maxLength": 128 },
                "document_revision": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 256,
                  "pattern": "^\\S+$"
                },
                "runtime_id": { "type": "string", "minLength": 1, "maxLength": 64 },
                "runtime_role": { "const": "primary" },
                "host_family": { "type": "string", "minLength": 1, "maxLength": 32 },
                "host_version": { "type": "string", "minLength": 1, "maxLength": 64 },
                "package_id": { "type": "string", "minLength": 1, "maxLength": 128 },
                "package_version": { "type": "string", "minLength": 1, "maxLength": 64 },
                "package_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
                "capability_manifest_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
                "operation_registry_version": { "type": "string", "minLength": 1, "maxLength": 64 },
                "operation_registry_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
                "policy_version": { "type": "string", "minLength": 1, "maxLength": 64 }
              }
            },
            "preview_binding": {
              "type": "object",
              "additionalProperties": false,
              "required": ["preview_id", "preview_digest"],
              "properties": {
                "preview_id": { "type": "string", "minLength": 1, "maxLength": 128 },
                "preview_digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" }
              }
            },
            "validation": {
              "type": "object",
              "additionalProperties": false,
              "required": ["validation_id", "receipt_id"],
              "properties": {
                "validation_id": { "type": "string", "minLength": 1, "maxLength": 128 },
                "receipt_id": { "type": "string", "minLength": 1, "maxLength": 128 },
                "expected_entity_count": { "type": "integer", "minimum": 0, "maximum": 256 },
                "expected_entity_types": {
                  "type": "array",
                  "maxItems": 16,
                  "uniqueItems": true,
                  "items": { "type": "string", "minLength": 1, "maxLength": 64 }
                },
                "expected_layers": {
                  "type": "array",
                  "maxItems": 64,
                  "uniqueItems": true,
                  "items": { "type": "string", "minLength": 1, "maxLength": 255 }
                }
              }
            }
          }
        }
      }
    },
    "result": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "operation_id", "result", "runtime_evidence"],
      "properties": {
        "status": { "enum": ["succeeded", "duplicate"] },
        "operation_id": {
          "enum": [
            "host.health",
            "drawing.observe.summary",
            "entity.snapshot.page",
            "document.events.summary",
            "cad.program.preview",
            "cad.program.commit",
            "cad.program.validate",
            "cad.recovery.receipt_query",
            "cad.rollback.checkpoint.lookup",
            "cad.rollback.preview",
            "cad.rollback.commit",
            "cad.rollback.validate"
          ]
        },
        "result": { "type": "object", "maxProperties": 64 },
        "runtime_evidence": {
          "type": "object",
          "additionalProperties": false,
          "required": ["runtime_id", "runtime_role", "host_family", "host_version"],
          "properties": {
            "runtime_id": { "const": "managed_dotnet" },
            "runtime_role": { "const": "primary" },
            "host_family": { "type": "string", "minLength": 1, "maxLength": 32 },
            "host_version": { "type": "string", "minLength": 1, "maxLength": 64 },
            "package_hash": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" }
          }
        }
      }
    },
    "error": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "error_code"],
      "properties": {
        "status": { "const": "failed" },
        "error_code": {
          "enum": [
            "invalid_envelope",
            "protocol_mismatch",
            "session_rejected",
            "deadline_expired",
            "payload_mismatch",
            "duplicate_payload_mismatch",
            "capability_missing",
            "no_active_document",
            "autocad_busy",
            "modal_dialog_active",
            "active_document_changed",
            "stale_snapshot",
            "stale_event_cursor",
            "program_invalid",
            "preview_required",
            "runtime_changed",
            "document_changed",
            "preview_abort_failed",
            "commit_validation_failed",
            "outcome_unknown",
            "rollback_conflict",
            "rollback_plan_expired",
            "ledger_corrupt",
            "ledger_full",
            "internal_error"
          ]
        },
        "error_message": { "type": "string", "maxLength": 2048 }
      }
    }
  }
}
