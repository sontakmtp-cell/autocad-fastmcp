# Scene-backed overall dimension

This version requires immutable exact contour or hole evidence for every
selected entity before the existing deterministic planner runs. The scene
cannot authorize a write: program preparation, preview, explicit user
continuation, trusted approval, commit, validation, recovery, and rollback
remain under the existing Phase 8 and Phase 9 authorities.
