[project]
name = "cad-core"
version = "0.1.0"
description = "MCP-independent CAD application service contracts"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cad_core"]
